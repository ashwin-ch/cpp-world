#include <iostream>
#include <vector>

using namespace std;

int main( int argc, char* argv[] )
{
    int numTc;
    vector<int> greenCost, purpleCost, numberOfParticipants ;
    int m, n;
  for( int i=0; i < numTc; ++i )
  {
    cin >> m >> n;
    std::cout << "dead ... " << std::endl;
    greenCost[i] = m;
    purpleCost[i] = n;
  }


  return 1;
}